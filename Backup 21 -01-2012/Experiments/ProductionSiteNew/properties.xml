<?xml version="1.0" encoding="UTF-8"?>
<project>

	<!-- name of web application -->
	<property name="proj.webapp.name" value="CSERT" />
    <property name="proj.tomcat.home" value="/home/udoluweera/Servers/production-site/apache-tomcat-5.5.28/" />

    <property name="warFileName" value="ROOT" />

	<!-- source directory structure -->
	<property name="proj.java.dir" location="${basedir}/src" />
	<property name="proj.webapp.dir" location="${basedir}/war" />
    <property name="proj.res.dir" location="${basedir}/res" />

	<!-- compile time only libraries -->
	<property name="proj.ext-lib.dir" location="${basedir}/libext" />

	<!-- target directory structure -->
	<property name="proj.dist.dir" value="${basedir}/dist" />

    <property name="build.sysclasspath" value="ignore" />
	<property name="ant.build.javac.source" value="1.6" />
    <property name="ant.build.javac.target" value="1.6" />

    <!-- Should Java compilations set the debug compiler option? -->
    <property name="proj.compile.debug" value="true" />

	<!-- JVM arguments for size of memory -->
    <property name="proj.runtime.initmemory" value="-Xms128M" />
	<property name="proj.runtime.maxmemory" value="-Xmx800M" />
    <property name="proj.runtime.permgensize" value="-XX:MaxPermSize=256M" />


</project>

